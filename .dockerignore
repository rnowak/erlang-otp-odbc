# Ignore everything by default
*

# Include only what the test runner needs
!rebar.config
!rebar.lock
!src/
!c_src/
!include/
!test/

# Exclude build artifacts that leak from host
c_src/*.o
priv/
_build/
